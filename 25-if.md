---
theme: penguin
class: text-center
highlighter: shiki
lineNumbers: true
drawings:
  persist: false
transition: slide-left
title: "@if 條件顯示"
routeAlias: ch25
style: |
  .slidev-layout p,
  .slidev-layout li,
  .slidev-layout td,
  .slidev-layout th,
  .slidev-layout div {
    font-size: max(16px, 1em);
  }
  table {
    width: 100%;
    margin: 1rem 0;
    border-collapse: collapse;
  }
  th, td {
    padding: 8px !important;
    border: 1px solid #e2e8f0 !important;
  }
  .index-table td {
    text-align: center;
    font-family: monospace;
  }
---

<div class="flex flex-col justify-center items-center h-full" style="background: #ffffff;">
  <p style="color: #5eada0; font-size: 1rem; font-weight: 600; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 1.2rem;">
    Angular Essentials
  </p>
  <h1 style="color: #1a5c5c; font-size: 3.8rem; font-weight: 900; line-height: 1.15; margin-bottom: 1.5rem;">
    @if 條件顯示
  </h1>
  <div style="height: 4px; width: 320px; background: linear-gradient(90deg, #5eada0, #a7d9d0); border-radius: 2px; margin-bottom: 1.5rem;"></div>
  <p style="color: #4a7c7c; font-size: 1.15rem; font-style: italic;">
    「根據條件動態決定畫面顯示與否」
  </p>
  <Link to="home" style="color: #9dc4c4; font-size: 0.85rem; margin-top: 2rem; text-decoration: none; letter-spacing: 0.05em;">← 返回目錄</Link>
</div>

---
layout: default
---

# Outline

- **什麼是 @if** — Angular 模板中的條件判斷語法
- **為什麼要用 @if** — @if 與 CSS display:none 的差異
- **@else / @else if** — 搭配 else 與 else if 處理多個條件分支

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 什麼是 @if？
# What is @if?

---

# 什麼是 @if？

@if 是 Angular 畫面用來判斷這段頁面是否顯示的代碼，跟 @for 一樣都是藉由你寫程式來讓你的畫面顯示的內容有所更改，寫法跟在 TypeScript 中的寫法是差不多的。

```html
@if (條件) {
  <!-- 條件成立時顯示的內容 -->
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>注意：</b> @if 是 Angular 17+ 引入的新語法，用來取代舊版的 <code>*ngIf</code> 結構型指令。
</div>

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 為什麼要用 @if？
# Why Use @if?

---

# 為什麼要用 @if？

當你的畫面顯示的時候需要根據資料（變數）來判斷你的畫面顯示，不過畫面顯示其實你也可以使用 CSS 來設置讓畫面是否顯示，這兩個的差異是在於 @if 隱藏是直接將你的這段畫面的內容直接移除而 CSS 設置只是將其讓使用者無法查看但是還是會顯示在底層中。

| 方式 | 說明 | DOM 是否存在 |
| --- | --- | --- |
| `@if (false)` | 條件不成立，內容從 DOM 中移除 | 否 |
| `style="display: none;"` | CSS 隱藏，內容仍在 DOM 中 | 是 |

```html
@if (false) {
  <h3>我是h3的內容</h3>
}

<h3 style="display: none;">我是h3的內容</h3>
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>安全性提示：</b> 使用 @if 移除 DOM 元素更安全，可防止使用者透過開發者工具看到隱藏內容。
</div>

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# @else / @else if
# Else Branches

---

# @else / @else if

當然有 if 就會有 else 跟 else if，畫面上的寫法跟程式寫法一樣。

```html
@if (false) {
  <h3>我是h3的內容</h3>
} @else {
  <h4>我是h4的內容</h4>
}
```

搭配 @else if 處理多個條件：

```html
@if (score >= 90) {
  <p>優秀</p>
} @else if (score >= 60) {
  <p>及格</p>
} @else {
  <p>不及格</p>
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>注意：</b> @else 與 @else if 必須緊接在 @if 或上一個 @else if 的右大括號 <code>}</code> 之後。
</div>

---
layout: end
---

# 課程結束
### 善用 @if 讓畫面根據資料動態顯示，比 CSS 隱藏更乾淨安全
