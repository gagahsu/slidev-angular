---
theme: penguin
class: text-center
highlighter: shiki
lineNumbers: true
drawings:
  persist: false
transition: slide-left
title: "@if 條件顯示"
routeAlias: ch27
style: |
  .slidev-layout p,
  .slidev-layout li,
  .slidev-layout td,
  .slidev-layout th,
  .slidev-layout div {
    font-size: max(16px, 1em);
  }
  table {
    width: 100%;
    margin: 1rem 0;
    border-collapse: collapse;
  }
  th, td {
    padding: 8px !important;
    border: 1px solid #e2e8f0 !important;
  }
  .index-table td {
    text-align: center;
    font-family: monospace;
  }
---

<div class="flex flex-col justify-center items-center h-full" style="background: #ffffff;">
  <p style="color: #5eada0; font-size: 1rem; font-weight: 600; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 1.2rem;">
    Angular Essentials
  </p>
  <h1 style="color: #1a5c5c; font-size: 3.8rem; font-weight: 900; line-height: 1.15; margin-bottom: 1.5rem;">
    @if 條件顯示
  </h1>
  <div style="height: 4px; width: 320px; background: linear-gradient(90deg, #5eada0, #a7d9d0); border-radius: 2px; margin-bottom: 1.5rem;"></div>
  <p style="color: #4a7c7c; font-size: 1.15rem; font-style: italic;">
    「根據條件動態決定畫面顯示與否」
  </p>
  <Link to="home" style="color: #9dc4c4; font-size: 0.85rem; margin-top: 2rem; text-decoration: none; letter-spacing: 0.05em;">← 返回目錄</Link>
</div>

<!--
各位學員，歡迎來到「@if 條件顯示」的主題！
你想想看，今天你點進一個購物網站。
如果你還沒登入，右上角應該要顯示「登入/註冊」按鈕。
一旦你登入成功了，那個按鈕就要自動功成身退，換成顯示你的「大頭貼與暱稱」。
這種「在特定條件下，決定畫面上某些東西要不要露臉」的需求，在網頁開發裡幾乎無所不在。
今天，我們就來學習 Angular 17+ 控制流的另一位超強金剛——`@if`！
-->

---
layout: default
---

# Outline

- **什麼是 @if** — Angular 模板中的條件判斷語法
- **為什麼要用 @if** — @if 與 CSS display:none 的差異
- **@else / @else if** — 搭配 else 與 else if 處理多個條件分支

<!--
今天我們的條件控制作戰計畫如下：
首先，認識什麼是 `@if` 以及它的基本語法結構。
接著，我們要解開一個前端的世紀大謎團：「既然可以用 CSS `display:none` 隱藏東西，那為什麼我們還要大費周章用 `@if`？」
最後，學習如何像寫 TS 一樣，在 HTML 裡把 `@else` 與 `@else if` 多個分支流暢地接起來！
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 什麼是 @if？
# What is @if?

<!--
第一站，我們先來認識 `@if` 這個語法。
-->

---

# 什麼是 @if？

`@if` 是 Angular 模板中的條件判斷語法，根據條件決定該區塊是否渲染至 DOM，語法與 TypeScript 的 `if` 相同。

```html
@if (條件) {
  <!-- 條件成立時顯示的內容 -->
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>注意：</b> @if 是 Angular 17+ 引入的新語法，用來取代舊版的 <code>*ngIf</code> 結構型指令。
</div>

<!--
簡單來說，`@if` 就是我們在 HTML 模板裡的「決策大腦」。
它的用法跟你以前寫 JS、TS 的 `if` 長得幾乎一模一樣。
在括號 `()` 裡傳入條件，在花括號 `{}` 裡寫入當條件成立（等於 true）時，你想畫在畫面上的 HTML 內容。
在新版 Angular 17 之後，我們統一使用這個小老鼠 `@if` 的語法。
比起舊版那個有點難寫的 `*ngIf` 指令，現在的寫法簡直直覺太多了！
-->

---
layout: default
---

# @if — 小節練習

請實作以下需求：

1. 宣告 `isLoggedIn = true`
2. 用 `@if` / `@else` 讓畫面在 `isLoggedIn` 為 `true` 時顯示「歡迎回來！」，否則顯示「請先登入」

<!--
考察 @if / @else 基本語法，以及條件變數與 DOM 移除的概念。改為需求描述題，移除填空程式碼區塊。
-->

---
layout: default
---

# @if — 小節練習解答

```typescript
isLoggedIn = true;
```

```html
@if (isLoggedIn) {
  <p>歡迎回來！</p>
} @else {
  <p>請先登入</p>
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <code>@if (false)</code> 會直接從 DOM 移除該元素，使用者按 F12 也看不到隱藏內容，比 CSS <code>display:none</code> 更安全
</div>

<!--
isLoggedIn 改為 false，「歡迎回來！」的 p 標籤會從 DOM 完全消失，而不只是視覺隱藏。
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 為什麼要用 @if？
# Why Use @if?

<!--
接下來是面試非常愛考、也是實務上非常關鍵的問題：
「既然我可以用 CSS 下 `display: none` 來隱藏按鈕，那為什麼我還需要學習 `@if`？」
這兩個有差嗎？
-->

---

# 為什麼要用 @if？

根據資料動態控制畫面顯示時，可用 `@if` 或 CSS `display:none`。兩者的關鍵差異在於 `@if` 為 `false` 時會將該區塊從 DOM 中完全移除，而 CSS 隱藏只是視覺上不可見，內容仍存在於 DOM 中。

| 方式 | 說明 | DOM 是否存在 |
| --- | --- | --- |
| `@if (false)` | 條件不成立，內容從 DOM 中移除 | 否 |
| `style="display: none;"` | CSS 隱藏，內容仍在 DOM 中 | 是 |

```html
@if (false) {
  <h3>我是h3的內容</h3>
}

<h3 style="display: none;">我是h3的內容</h3>
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>安全性提示：</b> 使用 @if 移除 DOM 元素更安全，可防止使用者透過開發者工具看到隱藏內容。
</div>

<!--
大叔把這兩個隱藏方法做個生動的比喻。
CSS 的 `display: none` 隱藏，就像是「把一個小偷藏在桌子底下，然後拿塊布蓋起來」。
雖然外面的人肉眼看不到他（畫面不顯示）。
但其實這個小偷「還好端端地蹲在房間裡」（它依然存在於瀏覽器的 DOM 樹中）。
任何使用者只要按 F12 打開開發者工具，把那一行的 `display: none` 給勾掉，這個小偷（或者機密按鈕、VIP 專屬區塊）就會立刻跳出來！這有很大的資安隱憂！
而 Angular 的 `@if (false)`，就像是「用傳送門直接把這個人傳送到外太空銷毀」。
它是直接把這個 HTML 標籤從 DOM 樹上「連根拔除」，瀏覽器裡編譯後根本沒有這行代碼。
不只更安全，瀏覽器渲染時也更省力、效能更好！
這就是為什麼，除非你是要做頻繁的淡入淡出動畫，否則決定顯示與否，一律優先用 `@if`！
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# @else / @else if
# Else Branches

<!--
那如果我們有多個條件，要怎麼在 HTML 裡做多選一的判斷呢？
-->

---

# @else / @else if

`@else` 與 `@else if` 的寫法與 TypeScript 相同，用於處理多個條件分支。

```html
@if (false) {
  <h3>我是h3的內容</h3>
} @else {
  <h4>我是h4的內容</h4>
}
```

搭配 @else if 處理多個條件：

```html
@if (score >= 90) {
  <p>優秀</p>
} @else if (score >= 60) {
  <p>及格</p>
} @else {
  <p>不及格</p>
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>注意：</b> @else 與 @else if 必須緊接在 @if 或上一個 @else if 的右大括號 <code>}</code> 之後。
</div>

<!--
這時候我們就可以把 `@else` 和 `@else if` 給接在後面。
用法跟寫 TS 一模一樣，非常流暢。
大叔特別提醒大家一個寫扣時的排版陷阱：
**你的 `@else` 或者是 `@else if`，必須緊緊貼在上一個右花括號 `}` 的正後方**！
如果你換行寫，或者中間隔了其他不相干的 HTML 標籤，編譯器就會當場黑臉、拋出編譯錯誤。
只要注意好這個括號要緊緊相依的習慣，你就能在 HTML 裡隨心所欲地控制各種複雜的畫面邏輯了！
-->

---
layout: default
---

# @else if — 小節練習

宣告 `score = 75`，用 `@if`、`@else if`、`@else` 顯示對應成績等級：

- 90 分以上：`優秀`
- 70 分以上：`良好`
- 60 分以上：`及格`
- 其他：`不及格`

<!--
考察多層 @else if 的語法結構，以及條件由高到低排列的重要性。
-->

---
layout: default
---

# @else if — 小節練習解答

```typescript
score = 75;
```

```html
@if (score >= 90) {
  <p>優秀</p>
} @else if (score >= 70) {
  <p>良好</p>    <!-- score = 75，顯示此行 -->
} @else if (score >= 60) {
  <p>及格</p>
} @else {
  <p>不及格</p>
}
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <code>@else if</code> 必須緊接在前一個 <code>&#125;</code> 之後，不能插入其他 HTML；條件由高到低排列，第一個符合的分支執行後即停止
</div>

<!--
條件順序很重要：若把 >= 60 放在 >= 90 前面，75 分就會先匹配到及格而不是良好。
-->

---
layout: end
---

# 課程結束
### 善用 @if 讓畫面根據資料動態顯示，比 CSS 隱藏更乾淨安全

<!--
恭喜大家！成功學會了用 `@if` 靈活地掌控畫面的生存與滅亡！
你現在的網頁已經非常有彈性，能夠根據登入狀態、分數高低來呈現不同的樣貌。
回去多寫幾個條件控制的練習。
下一堂課，我們要來學習 `@if` 的好兄弟——`@switch` 語法，去看看在分支條件特別多的時候，怎麼寫能比 `@else if` 還要更乾淨！大家休息一下，我們等一下見！
-->
